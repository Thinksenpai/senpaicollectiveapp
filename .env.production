# Production API base URL — Vite bakes this into the build during `npm run build`.
# The backend serves its routes under /api (e.g. /api/auth/login), so include it here.
VITE_API_BASE_URL=https://api.senpaicollective.com/api
